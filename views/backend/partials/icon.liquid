{% comment %} Lucide 开源图标（MIT）。用法：{% include 'backend/partials/icon', name: 'dashboard', cls: 'kt-menu-icon' %}；默认 class 为 kt-menu-icon。渲染为 <i data-lucide> 由 lucide.min.js 的 createIcons() 替换为内联 SVG（离线、无运行时 CDN 依赖）。{% endcomment %}
<i data-lucide="{{ name }}" class="{{ cls | default: 'kt-menu-icon' }}"></i>
