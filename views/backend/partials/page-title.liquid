<div class="kt-card mb-6">
  <div class="kt-card-content py-3">
    <div class="flex flex-wrap items-center justify-between gap-2">
      <h4 class="text-base font-semibold text-default-900">{{ title }}</h4>
      <div class="hidden items-center gap-2 text-sm font-medium md:flex">
        <a href="/console/" class="text-default-700">控制台</a>
        <i data-lucide="chevron-right" class="size-4 text-default-500"></i>
        <span class="text-default-700">{{ subtitle }}</span>
        <i data-lucide="chevron-right" class="size-4 text-default-500"></i>
        <span class="text-default-500" aria-current="page">{{ title }}</span>
      </div>
    </div>
  </div>
</div>
